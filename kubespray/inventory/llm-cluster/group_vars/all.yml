ansible_user: ubuntu #using the same account for the machine login, nice to have a dedicated user for ansible.
ansible_ssh_private_key_file: ~/.ssh/id_ed25519
ansible_become: true  #used it here is get inherited by all the tasks
ansible_ssh_common_args: '-J root@192.168.122.219' #jump via proxmox to get to the VMs